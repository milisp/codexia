# 0.48.1 - 2026-08-19

[Compare with 0.48.0](https://github.com/milisp/codexia/compare/v0.48.0...v0.48.1)

## Added

- Codex thread item components are localized, so their labels follow the selected language (`9b8d918e`).

## Fixed

- Diff counts stay next to the file path in file change summaries (`3584d298`).
- Wide agent messages scroll horizontally instead of overflowing the layout (`6543b4b5`).
- Hot reload no longer falls back to a full page reload and drops in-flight events (`a264340e`).

## Internal

Session listener readiness and agent option generation are centralized in `useCCSessionManager`, and the README hero image now points at a remote asset URL.
