# 0.47.1 - 2026-08-18

[Compare with 0.47.0](https://github.com/milisp/codexia/compare/v0.47.0...v0.47.1)

## Fixed

- Codex permission escalation requests are forwarded to the UI, so commands needing elevated access prompt instead of stalling (`b7d26094`).
- MCP elicitation requests from Codex are forwarded to the UI, so servers asking for input can be answered (`00df9b16`).
- Provider environment variables are passed to the `app-server` child process, so custom providers get their API keys (`b61cce10`).
